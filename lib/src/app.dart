import 'package:flutter/material.dart';

class App extends StatelessWidget {
  const App({super.key});

  Widget build(context){
return MaterialApp(
title: 'Log Me In Please!',
home: Scaffold(
  body: Text('Please make a login form here!'),
),
);
  }
}